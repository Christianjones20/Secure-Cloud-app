name:Docker Build Check

on:
  push:
    branches:
        - main
    pull_requests:
    branches:
        - main

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@4
      - name: Build Docker Image
        run: docker build -t cloud-status-app:v1
